# database.py — the data layer. Knows SQLite, knows nothing about Tkinter.

# Job 1: create the cards table if it doesn't already exist
#        columns: id (auto), domain (1-5), front (text), back (text)

# Job 2: add a card (takes domain, front, back — id is automatic)

# Job 3: get all cards (returns every row)

# Job 4: get one random card (returns a single random row — for the quiz)